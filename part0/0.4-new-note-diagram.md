```mermaid
sequenceDiagram
    participant User
    participant Browser
    participant Server

    Note left of User: User types a note and clicks Save

    User->>Browser: Submit note form
    Browser->>Server: POST /new_note with form data
    activate Server
    Server-->>Browser: HTTP 302 Redirect to /notes
    deactivate Server

    Browser->>Server: GET /notes
    activate Server
    Server-->>Browser: HTML page with form and notes
    deactivate Server

    Browser->>Server: GET /main.css
    Server-->>Browser: CSS styles

    Browser->>Server: GET /main.js
    Server-->>Browser: JavaScript app logic

    Browser->>Server: GET /data.json
    Server-->>Browser: JSON with updated note list

    Note right of Browser: Browser renders page from scratch, including the new note
